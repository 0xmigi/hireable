---
description: List the named commands the hireable skill exposes
---

Activate the hireable skill and print its Commands table — list every named operation (`hireable ingest`, `hireable autofill`, `hireable radar`, `hireable tailor`, `hireable help`) with its trigger phrases and a one-line description of what it does. Read the table directly from `SKILL.md` rather than recomposing it from memory. Don't run any of the commands, just describe what's available.
